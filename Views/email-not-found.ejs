<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Email Not Found</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <style>
        body {
            background: linear-gradient(135deg, #2C3E50, #009150);
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            font-family: 'Poppins', sans-serif;
            overflow: hidden;
        }

        .card {
            background: #ffffff;
            border-radius: 12px;
            padding: 30px;
            width: 400px;
            text-align: center;
            box-shadow: 0px 4px 15px rgba(0, 0, 0, 0.2);
            animation: fadeIn 1s ease-in-out;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: scale(0.9);
            }

            to {
                opacity: 1;
                transform: scale(1);
            }
        }

        .card h3 {
            color: #D9534F;
            font-weight: bold;
            margin-bottom: 15px;
        }

        .card p {
            color: #555;
        }

        .btn-primary {
            background: #D9534F;
            border: none;
            height: 45px;
            border-radius: 8px;
            font-weight: bold;
        }

        .btn-primary:hover {
            background: #C9302C;
        }

        .btn-secondary {
            border-radius: 8px;
            height: 45px;
            font-weight: bold;
        }
    </style>
</head>

<body>

    <div class="card">
        <h3>Email Not Found!</h3>
        <p>The email you entered does not exist in our records. Please check your email and try again.</p>
        <a href="/admin/forget-password" class="btn btn-primary w-100 mb-3">Try Again</a>

    </div>

</body>

</html>