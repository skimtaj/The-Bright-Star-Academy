<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Attendance Form</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #E3F2FD;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }

        .attendance-form {
            background: white;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.2);
            width: 100%;
            max-width: 500px;
        }

        .attendance-logo {
            width: 100px;
            height: 100px;
            margin-bottom: 10px;
        }

        .btn-custom {
            background-color: #00A65A;
            border: none;
            color: white;
        }

        .btn-custom:hover {
            background-color: #008C4A;
        }

        .form-label {
            font-weight: bold;
        }

        .form-intro {
            text-align: center;
            margin-bottom: 15px;
            font-size: 16px;
            color: #555;
        }

        .underline {
            width: 100%;
            height: 2px;
            background-color: #00A65A;
            margin-bottom: 20px;
        }

        .back-btn {
            background-color: #6C757D;
            color: white;
            border: none;
        }

        .back-btn:hover {
            background-color: #5A6268;
        }
    </style>
</head>

<body>
    <div class="attendance-form">
        <div class="text-center">
            <img src="https://cdn-icons-png.flaticon.com/512/2921/2921222.png" alt="Professional Attendance Logo"
                class="attendance-logo">
            <h3 class="mb-2">Attendance Form</h3>
            <p class="form-intro">
                Mark attendance by filling out the details below.
            </p>
            <div class="underline"></div>
        </div>
        <form method="post">

            <div class="mb-3">
                <label class="form-label">Date</label>
                <input type="date" class="form-control" required name="date" id="Date" name="date" disabled>
            </div>
            <div class="mb-3">
                <label class="form-label">Status</label>
                <select class="form-control" required name="Status">
                    <option value="">Select</option>
                    <option>Present</option>
                    <option>Absent</option>
                    <option>Late</option>
                    <option>On Leave</option>
                </select>
            </div>
            <div class="mb-3">
                <label class="form-label">Note (Optional)</label>
                <textarea class="form-control" rows="3" placeholder="Enter any remarks" name="Note"></textarea>
            </div>
            <button type="submit" class="btn btn-custom w-100" onclick="submitAttendance()">Submit Attendance</button>
            <button class="btn back-btn w-100 mt-2" onclick="history.back()">← Back</button>
        </form>
    </div>
</body>

<script>

    const date = document.getElementById('Date');


    window.onload = () => {

        date.value = new Date().toISOString().split('T')[0];

    }


    const submitAttendance = () => {

        date.disabled = false;

    }

</script>

</html>