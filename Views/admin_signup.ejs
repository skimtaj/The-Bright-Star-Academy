<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Registration</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #E3F2FD;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }

        .admin-form {
            background: white;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.2);
            width: 100%;
            max-width: 500px;
        }

        .admin-logo {
            width: 100px;
            height: 100px;
            margin-bottom: 10px;
        }

        .btn-custom {
            background-color: #00A65A;
            border: none;
            color: white;
        }

        .btn-custom:hover {
            background-color: #008C4A;
        }

        .form-label {
            font-weight: bold;
        }

        .form-intro {
            text-align: center;
            margin-bottom: 15px;
            font-size: 16px;
            color: #555;
        }

        .underline {
            width: 100%;
            height: 2px;
            background-color: #00A65A;
            margin-bottom: 20px;
        }

        .back-btn {
            background-color: #6C757D;
            color: white;
            border: none;
        }

        .back-btn:hover {
            background-color: #5A6268;
        }
    </style>
</head>

<body>
    <div class="admin-form">
        <div class="text-center">
            <img src="https://cdn-icons-png.flaticon.com/512/3135/3135715.png" alt="Admin Logo" class="admin-logo">
            <h3 class="mb-2">Admin Registration</h3>
            <p class="form-intro">
                Fill in the details below to register a new admin.
            </p>
            <div class="underline"></div>
        </div>
        <form method="post" enctype="multipart/form-data">

            <div class="mb-3">
                <label class="form-label">Full Name</label>
                <input type="text" class="form-control" required name="Name" placeholder="Enter full name">
            </div>

            <div class="mb-3">
                <label class="form-label">Mobile</label>
                <input type="tel" class="form-control" required name="Mobile" placeholder="Enter mobile number">
            </div>

            <div class="mb-3">
                <label class="form-label">Email</label>
                <input type="email" class="form-control" required name="Email" placeholder="Enter email">
            </div>

            <div class="mb-3">
                <label class="form-label">Password</label>
                <input type="password" class="form-control" required name="Password" placeholder="Enter password">
            </div>

            <div class="mb-3">
                <label class="form-label">Profile Image</label>
                <input type="file" class="form-control" required name="Profile_Image">
            </div>

            <button type="submit" class="btn btn-custom w-100">Register</button>
            <a href="/TBSA/admin-login" class="btn back-btn w-100 mt-2">← Back</a>
        </form>
    </div>
</body>

</html>