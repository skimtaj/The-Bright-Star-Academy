<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Registration Page</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        body {
            margin: 30px 25px;
            padding: 0;
            background: linear-gradient(135deg, #6C63FF, #3A4DF3);
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            font-family: 'Poppins', sans-serif;
        }

        .register-container {
            background: #ffffff;
            width: 100%;
            max-width: 450px;
            padding: 35px 25px;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            position: relative;
            overflow: hidden;
        }

        .register-header {
            text-align: center;
            margin-bottom: 25px;
        }

        .register-header .icon {
            font-size: 40px;
            color: #3A4DF3;
            margin-bottom: 10px;
        }

        .register-header h1 {
            font-size: 26px;
            color: #3A4DF3;
            margin: 0;
        }

        .register-header p {
            font-size: 14px;
            color: #555;
            margin-top: 5px;
        }

        .form-group {
            margin-bottom: 15px;
            position: relative;
        }

        .form-group input,
        .form-group select {
            width: 100%;
            padding: 12px;
            border: 1px solid #ccc;
            border-radius: 8px;
            font-size: 14px;
            color: #555;
            background: #f9f9f9;
            transition: border 0.3s;
        }

        .form-group input:focus,
        .form-group select:focus {
            border-color: #3A4DF3;
            background: #fff;
        }

        .form-group label {
            position: absolute;
            top: 10px;
            left: 15px;
            font-size: 14px;
            color: #888;
            pointer-events: none;
            transition: all 0.3s;
        }

        .form-group input:focus+label,
        .form-group input:not(:placeholder-shown)+label,
        .form-group select:focus+label,
        .form-group select:not([value=""])+label {
            top: -10px;
            left: 15px;
            background: #fff;
            padding: 0 5px;
            font-size: 12px;
            color: #3A4DF3;
        }

        .form-group .fa {
            position: absolute;
            top: 50%;
            right: 15px;
            transform: translateY(-50%);
            color: #aaa;
            cursor: pointer;
        }

        .btn-primary {
            width: 100%;
            padding: 12px;
            font-size: 15px;
            border: none;
            border-radius: 8px;
            background: #3A4DF3;
            color: #fff;
            font-weight: bold;
            transition: background 0.3s;
        }

        .btn-primary:hover {
            background: #6C63FF;
        }

        .footer-text {
            text-align: center;
            margin-top: 20px;
            font-size: 14px;
            color: #555;
        }

        .footer-text a {
            color: #3A4DF3;
            text-decoration: none;
            font-weight: bold;
        }

        .footer-text a:hover {
            text-decoration: underline;
        }

        .alert {
            font-size: 14px;
        }

        @media (max-width: 576px) {
            .register-container {
                padding: 30px 20px;
            }

            .btn-primary {
                padding: 10px;
                font-size: 14px;
            }
        }
    </style>
</head>

<body>
    <div class="register-container">
        <div class="register-header">
            <i class="fas fa-user-plus icon"></i>
            <h1>Sign Up</h1>
            <p>Create your admin account quickly and securely</p>
        </div>

        <% if (messages.success) { %>
            <div class="alert alert-success" id="msg">
                <%= messages.success %>
            </div>
            <% } %>

                <% if (messages.error) { %>
                    <div class="alert alert-danger" id="msg">
                        <%= messages.error %>
                    </div>
                    <% } %>

                        <form method="post" enctype="multipart/form-data">
                            <div class="form-group">
                                <input type="text" id="name" name="Name" placeholder=" " value="<%=adminSourse.Name%>">
                                <label for="name">Full Name</label>
                            </div>

                            <div class="form-group">
                                <input type="number" id="mobile" name="Mobile" placeholder=" "
                                    value="<%=adminSourse.Mobile%>">
                                <label for="mobile">Mobile</label>
                            </div>

                            <div class="form-group">
                                <input type="email" id="email" name="Email" placeholder=" "
                                    value="<%=adminSourse.Email%>">
                                <label for="email">Email Address</label>
                            </div>



                            <div class="form-group">
                                <input type="file" id="profileImage" name="Profile_Image" accept="image/*">
                                <label for="profileImage">Profile Image</label>
                            </div>


                            <button type="submit" class="btn btn-primary">Update</button>



                            <center> <a href="/admin-login" class="btn btn-secondary mb-3 btn-sm">
                                    <i class="fas fa-arrow-left"></i> Back
                                </a></center>

                        </form>
    </div>

    <script>


        setTimeout(() => {
            const msg = document.getElementById('msg');
            if (msg) msg.style.display = 'none';
        }, 6000);
    </script>
</body>

</html>