<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Teacher Registration Form</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #E3F2FD;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }

        .registration-form {
            background: white;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.2);
            width: 100%;
            max-width: 700px;
        }

        .profile-logo {
            width: 80px;
            height: 80px;
            margin-bottom: 10px;
        }

        .btn-custom {
            background-color: #00A65A;
            border: none;
            color: white;
        }

        .btn-custom:hover {
            background-color: #008C4A;
        }

        .form-label {
            font-weight: bold;
        }

        .form-intro {
            text-align: center;
            margin-bottom: 15px;
            font-size: 16px;
            color: #555;
        }

        .underline {
            width: 100%;
            height: 2px;
            background-color: #00A65A;
            margin-bottom: 20px;
        }

        .section-box {
            background: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.15);
            margin-bottom: 20px;
        }

        .profile-preview {
            display: flex;
            flex-direction: column;
            align-items: center;
            margin-bottom: 10px;
        }

        .profile-preview img {
            width: 100px;
            height: 100px;
            border-radius: 50%;
            object-fit: cover;
            border: 2px solid #00A65A;
            display: none;
        }
    </style>
</head>

<body>
    <div class="registration-form">
        <div class="text-center">
            <img src="https://cdn-icons-png.flaticon.com/512/3135/3135715.png" alt="Profile Logo" class="profile-logo">
            <h3 class="mb-2">Edit Teacher Profile </h3>
            <p class="form-intro">
                Edit Tecaher profile with very carefully.
            </p>


            <% if (messages.success) { %>
                <div class="alert alert-success" id="msg">
                    <%= messages.success %>
                </div>
                <% } %>

                    <% if (messages.error) { %>
                        <div class="alert alert-danger" id="msg">
                            <%= messages.error %>
                        </div>
                        <% } %>


                            <div class="underline"></div>
        </div>
        <form method="post" enctype="multipart/form-data">
            <div class="section-box">
                <h5 class="mb-3">Personal Details</h5>

                <div class="profile-preview">
                    <img id="profileImagePreview" alt="Profile Preview">
                </div>


                <div class="row">
                    <div class="col-md-12 mb-3">
                        <label class="form-label"> Name</label>
                        <input type="text" class="form-control" placeholder="Enter Teacher Name" name="Name"
                            value="<%=teacherSourse.Name%>">
                    </div>

                </div>

                <div class="row">

                    <div class="col-md-6 mb-3">
                        <label class="form-label">Phone Number</label>
                        <input type="tel" class="form-control" placeholder="Enter phone number" name="Mobile"
                            value="<%=teacherSourse.Mobile%>">
                    </div>


                    <div class="col-md-6 mb-3">
                        <label class="form-label">Email</label>
                        <input type="email" class="form-control" placeholder="Enter email" name="Email"
                            value="<%=teacherSourse.Email%>">
                    </div>

                </div>

                <div class="row">
                    <div class="col-md-6 mb-3">
                        <label class="form-label">Date of Birth</label>
                        <input type="date" class="form-control" name="DOB" value="<%=teacherSourse.DOB%>">
                    </div>
                    <div class="col-md-6 mb-3">
                        <label class="form-label">Gender</label>
                        <select class="form-control" name="Gender">
                            <option value="">Select</option>
                            <option value="Male" <%=teacherSourse.Gender==='Male' ? 'selected' : '' %> >Male</option>
                            <option value="Female" <%=teacherSourse.Gender==='Female' ? 'selected' : '' %> >Female
                            </option>
                            <option value="Other" <%=teacherSourse.Gender==='Other' ? 'selected' : '' %> >Other</option>
                        </select>
                    </div>
                </div>
                <div class="mb-3">
                    <label class="form-label">Address</label>
                    <input type="text" class="form-control" placeholder="Street Address" name="Address"
                        value="<%=teacherSourse.Address%>">
                </div>

                <div class="mb-3">
                    <label class="form-label">Upload Profile Image</label>
                    <input type="file" class="form-control" accept="image/*" id="profileImageInput"
                        name="Profile_Image">
                </div>


            </div>

            <div class="section-box">
                <h5 class="mb-3">Professional Details</h5>
                <div class="mb-3">
                    <label class="form-label">Qualifications</label>
                    <input type="text" class="form-control" placeholder="Degrees, certifications, etc."
                        name="Qualifications" value="<%=teacherSourse.Qualifications%>">
                </div>
                <div class="mb-3">
                    <label class="form-label">Teaching Experience (Years)</label>
                    <input type="number" class="form-control" placeholder="Teaching Experience" name="Experience"
                        value="<%=teacherSourse.Experience%>">
                </div>
                <div class="mb-3">
                    <label class="form-label">Specializations</label>
                    <input type="text" class="form-control" placeholder="Subjects, age groups, etc."
                        name="Specializations" value="<%=teacherSourse.Specializations%>">
                </div>
                <div class="mb-3">
                    <label class="form-label">Previous Institution/School (Optional)</label>
                    <input type="text" class="form-control" placeholder="Enter institution name"
                        name="previousInstitute" value="<%=teacherSourse.previousInstitute%>">
                </div>
            </div>



            <button type="submit" class="btn btn-custom w-100">Update</button>
            <a href="/TBSA/admin-dashboard" type="button" class="btn btn-secondary w-100 mt-2">Back</a>

        </form>
    </div>

</body>

<script>

    setTimeout(() => {

        document.getElementById('msg').style.display = 'none'

    }, 3000)

</script>

</html>