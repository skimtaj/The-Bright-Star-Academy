<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile Account</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>
    <style>
        body {
            background: linear-gradient(135deg, #1A1A2E, #16213E);
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            font-family: 'Poppins', sans-serif;
        }

        .profile-card {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            border-radius: 15px;
            padding: 30px;
            width: 400px;
            text-align: center;
            box-shadow: 0px 5px 20px rgba(0, 0, 0, 0.3);
            border: 1px solid rgba(255, 255, 255, 0.2);
            animation: fadeIn 1s ease-in-out;
            color: white;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(-20px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .profile-img {
            width: 120px;
            height: 120px;
            border-radius: 50%;
            object-fit: cover;
            border: 5px solid #E94560;
            padding: 5px;
            margin-bottom: 15px;
            background: linear-gradient(135deg, #E94560, #FF9A8B);
        }

        .profile-card h3 {
            font-weight: bold;
            color: #FFD700;
            margin-bottom: 10px;
        }

        .profile-info {
            text-align: left;
            font-size: 16px;
            margin-bottom: 10px;
            padding: 10px;
            border-radius: 8px;
            background: rgba(255, 255, 255, 0.2);
            display: flex;
            align-items: center;
        }

        .profile-info i {
            color: #FFD700;
            margin-right: 12px;
            font-size: 18px;
        }

        .btn-custom {
            background: #E94560;
            border: none;
            height: 45px;
            border-radius: 8px;
            font-weight: bold;
            color: white;
            transition: 0.3s;
        }

        .btn-custom:hover {
            background: #FF9A8B;
            transform: scale(1.05);
        }

        .btn-secondary {
            border-radius: 8px;
            height: 45px;
            font-weight: bold;
            color: white;
            background: rgba(255, 255, 255, 0.2);
            border: 1px solid rgba(255, 255, 255, 0.3);
            transition: 0.3s;
        }

        .btn-secondary:hover {
            background: rgba(255, 255, 255, 0.3);
            transform: scale(1.05);
        }
    </style>
</head>

<body>

    <div class="profile-card">
        <img src="/uploads/<%=adminSourse.Profile_Image%>" alt="Profile Image" class="profile-img">
        <h3>John Doe</h3>

        <div class="profile-info"><i class="fas fa-user"></i> <strong>Name:</strong>
            <%=adminSourse.Name%>
        </div>
        <div class="profile-info"><i class="fas fa-phone-alt"></i> <strong>Mobile:</strong> +1 234 567 890</div>
        <div class="profile-info"><i class="fas fa-envelope"></i> <strong>Email:</strong>
            <%=adminSourse.Email%>
        </div>

        <a href="/admin-dashboard/edit-profile/<%=adminSourse._id%>" class="btn btn-custom w-100 mt-3"><i
                class="fas fa-edit"></i> Update Profile</a>
        <button class="btn btn-secondary w-100 mt-2" onclick="history.back()"><i class="fas fa-arrow-left"></i> Go
            Back</button>
    </div>

</body>

</html>