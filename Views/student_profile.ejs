<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Student Profile | <%=studentProfile.Name%></title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/css/bootstrap.min.css">
    <style>
        :root {
            --primary: #4e73df;
            --primary-dark: #224abe;
            --secondary: #1cc88a;
            --secondary-dark: #17a673;
            --dark: #3a3b45;
            --light: #f8f9fc;
            --border: #e3e6f0;
            --warning: #f6c23e;
            --danger: #e74a3b;
            --success-light: rgba(28, 200, 138, 0.1);
            --shadow-sm: 0 0.125rem 0.25rem rgba(0, 0, 0, 0.075);
            --shadow: 0 0.15rem 1.75rem rgba(58, 59, 69, 0.1);
        }

        body {
            font-family: 'Nunito', -apple-system, BlinkMacSystemFont, sans-serif;
            background-color: #f5f7fb;
            color: var(--dark);
            line-height: 1.5;
        }

        .profile-container {
            max-width: 1000px;
            margin: 2rem auto;
            padding: 0 15px;
        }

        .profile-card {
            background: white;
            border-radius: 0.5rem;
            box-shadow: var(--shadow);
            overflow: hidden;
        }

        .profile-header {
            background: linear-gradient(135deg, var(--primary) 0%, var(--primary-dark) 100%);
            color: white;
            padding: 2rem 1.5rem;
            text-align: center;
        }

        .profile-avatar {
            width: 120px;
            height: 120px;
            border-radius: 50%;
            border: 4px solid rgba(255, 255, 255, 0.3);
            object-fit: cover;
            margin: 0 auto 1rem;
            box-shadow: var(--shadow-sm);
        }

        .section {
            padding: 1.5rem;
            border-bottom: 1px solid var(--border);
        }

        .section:last-child {
            border-bottom: none;
        }

        .section-title {
            color: var(--primary-dark);
            font-weight: 700;
            margin-bottom: 1.25rem;
            font-size: 1.25rem;
            display: flex;
            align-items: center;
        }

        .section-title i {
            margin-right: 0.75rem;
            font-size: 1.1rem;
        }

        .detail-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
            gap: 1rem;
        }

        .detail-item {
            display: flex;
            margin-bottom: 0.75rem;
        }

        .detail-icon {
            color: var(--primary);
            margin-right: 0.75rem;
            min-width: 24px;
            text-align: center;
            padding-top: 0.15rem;
        }

        .detail-content {
            flex: 1;
        }

        .detail-label {
            font-weight: 600;
            font-size: 0.85rem;
            color: #6c757d;
            margin-bottom: 0.15rem;
        }

        .detail-value {
            font-size: 0.95rem;
            word-break: break-word;
        }

        .btn {
            transition: all 0.2s;
            font-weight: 600;
        }

        .btn-back {
            background: white;
            color: var(--primary);
            border: 1px solid var(--primary);
        }

        .btn-back:hover {
            background: var(--primary);
            color: white;
        }

        .badge-status {
            padding: 0.35rem 0.75rem;
            border-radius: 50rem;
            font-size: 0.75rem;
            font-weight: 700;
            display: inline-flex;
            align-items: center;
        }

        .badge-verified {
            background-color: var(--success-light);
            color: var(--secondary);
        }

        .badge-pending {
            background-color: rgba(246, 194, 62, 0.1);
            color: var(--warning);
        }

        .receipt-form {
            display: flex;
            gap: 0.5rem;
            margin-top: 0.5rem;
        }

        .receipt-form .form-control {
            height: calc(1.5em + 0.75rem);
        }

        .action-buttons {
            display: flex;
            gap: 0.5rem;
        }

        @media (max-width: 767.98px) {
            .profile-header {
                padding: 1.5rem 1rem;
            }
            
            .section {
                padding: 1.25rem;
            }
            
            .detail-grid {
                grid-template-columns: 1fr;
            }
            
            .profile-avatar {
                width: 100px;
                height: 100px;
            }
        }

        @media (min-width: 992px) {
            .detail-grid {
                gap: 1.25rem;
            }
        }
    </style>
</head>

<body>
    <div class="profile-container">
        <div class="profile-card">
            <div class="profile-header">
                <img src="https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcSOH2aZnIHWjMQj2lQUOWIL2f4Hljgab0ecZQ&s"
                    alt="<%=studentProfile.Name%>" class="profile-avatar">
                <h2 class="h4 font-weight-bold mb-1">
                    <%=studentProfile.Name%>
                </h2>
                <p class="mb-0 text-white-50">
                    <%=studentProfile.admissionFor%> Student
                </p>
            </div>

            <div class="section">
                <h3 class="section-title"><i class="fas fa-user-circle"></i> Personal Information</h3>
                <div class="detail-grid">
                    <div class="detail-item">
                        <div class="detail-icon">
                            <i class="fas fa-user-tie"></i>
                        </div>
                        <div class="detail-content">
                            <div class="detail-label">Full Name</div>
                            <div class="detail-value">
                                <%=studentProfile.Name%>
                            </div>
                        </div>
                    </div>

                    <div class="detail-item">
                        <div class="detail-icon">
                            <i class="fas fa-user-friends"></i>
                        </div>
                        <div class="detail-content">
                            <div class="detail-label">Father's Name</div>
                            <div class="detail-value">
                                <%=studentProfile.GuardianName%>
                            </div>
                        </div>
                    </div>

                    <div class="detail-item">
                        <div class="detail-icon">
                            <i class="fas fa-mobile-alt"></i>
                        </div>
                        <div class="detail-content">
                            <div class="detail-label">Phone</div>
                            <div class="detail-value">
                                <%=studentProfile.Mobile%>
                            </div>
                        </div>
                    </div>

                    <div class="detail-item">
                        <div class="detail-icon">
                            <i class="fas fa-venus-mars"></i>
                        </div>
                        <div class="detail-content">
                            <div class="detail-label">Gender</div>
                            <div class="detail-value">
                                <%=studentProfile.Gender%>
                            </div>
                        </div>
                    </div>

                    <div class="detail-item">
                        <div class="detail-icon">
                            <i class="fas fa-birthday-cake"></i>
                        </div>
                        <div class="detail-content">
                            <div class="detail-label">Date of Birth</div>
                            <div class="detail-value">
                                <%=studentProfile.DOB%>
                            </div>
                        </div>
                    </div>

                    <div class="detail-item">
                        <div class="detail-icon">
                            <i class="fas fa-praying-hands"></i>
                        </div>
                        <div class="detail-content">
                            <div class="detail-label">Religion</div>
                            <div class="detail-value">
                                <%=studentProfile.Religion%>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <div class="section">
                <h3 class="section-title"><i class="fas fa-graduation-cap"></i> Academic Information</h3>
                <div class="detail-grid">
                    <div class="detail-item">
                        <div class="detail-icon">
                            <i class="fas fa-calendar-alt"></i>
                        </div>
                        <div class="detail-content">
                            <div class="detail-label">Academic Year</div>
                            <div class="detail-value">
                                <%=studentProfile.academicYear%>
                            </div>
                        </div>
                    </div>

                    <div class="detail-item">
                        <div class="detail-icon">
                            <i class="fas fa-school"></i>
                        </div>
                        <div class="detail-content">
                            <div class="detail-label">Class</div>
                            <div class="detail-value">
                                <%=studentProfile.admissionFor%>
                            </div>
                        </div>
                    </div>

                    <div class="detail-item">
                        <div class="detail-icon">
                            <i class="fas fa-id-badge"></i>
                        </div>
                        <div class="detail-content">
                            <div class="detail-label">Roll No</div>
                            <div class="detail-value">
                                <%=studentProfile.rollNo%>
                            </div>
                        </div>
                    </div>

                    <div class="detail-item">
                        <div class="detail-icon">
                            <i class="fas fa-money-bill-wave"></i>
                        </div>
                        <div class="detail-content">
                            <div class="detail-label">Monthly Fees</div>
                            <div class="detail-value">
                                ₹<%=studentProfile.totalMonthlyFees%>
                            </div>
                        </div>
                    </div>

                    <div class="detail-item">
                        <div class="detail-icon">
                            <i class="fas fa-file-certificate"></i>
                        </div>
                        <div class="detail-content">
                            <div class="detail-label">Previous Result</div>
                            <div class="detail-value">
                                <% if(studentProfile.PreviousResult) { %>
                                    <a href="/download-student-result/<%=studentProfile._id%>" class="btn btn-primary btn-sm">
                                        <i class="fas fa-download mr-1"></i> Download
                                    </a>
                                <% } else { %>
                                    <span class="text-muted">Not available</span>
                                <% } %>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <div class="section">
                <h3 class="section-title"><i class="fas fa-credit-card"></i> Payment Information</h3>
                <div class="detail-grid">
                    <div class="detail-item">
                        <div class="detail-icon">
                            <i class="fas fa-money-check-alt"></i>
                        </div>
                        <div class="detail-content">
                            <div class="detail-label">Payment Method</div>
                            <div class="detail-value">
                                <span class="badge-status">
                                    <%=studentProfile.paymentMethod%>
                                </span>
                            </div>
                        </div>
                    </div>

                    <div class="detail-item">
                        <div class="detail-icon">
                            <i class="fas fa-file-invoice-dollar"></i>
                        </div>
                        <div class="detail-content">
                            <div class="detail-label">Payment Proof</div>
                            <div class="detail-value">
                                <% if(studentProfile.paymentMethod==='Bank Transfer') { %>
                                    <a href="/studentBankproof/<%=studentProfile._id%>" class="btn btn-warning btn-sm text-white">
                                        <i class="fas fa-download mr-1"></i> Bank Proof
                                    </a>
                                <% } %>
                                
                                <% if(studentProfile.paymentMethod==='Cash') { %>
                                    <% if(studentProfile.Cash_Recipt_Number) { %>
                                        <div class="d-flex align-items-center">
                                            <span class="mr-2"><%=studentProfile.Cash_Recipt_Number%></span>
                                            
                                        </div>
                                    <% } else { %>
                                        <form action="/cash-payment-receipt/<%=studentProfile._id%>" method="post" class="receipt-form">
                                            <input type="text" name="Cash_Recipt_Number"
                                                class="form-control form-control-sm"
                                                placeholder="Receipt number"
                                                required>
                                            <input type="hidden" name="studentId"
                                                value="<%= studentProfile._id %>">
                                            <button type="submit" class="btn btn-primary btn-sm">
                                                Save
                                            </button>
                                        </form>
                                    <% } %>
                                <% } %>
                                
                                <% if(studentProfile.paymentMethod==='QR Code') { %>
                                    <a href="<%= studentProfile.qrPaymentProof %>" class="btn btn-warning btn-sm text-white">
                                        <i class="fas fa-download mr-1"></i> QR Payment
                                    </a>
                                <% } %>
                            </div>
                        </div>
                    </div>

                    <div class="detail-item">
                        <div class="detail-icon">
                            <i class="fas fa-check-circle"></i>
                        </div>
                        <div class="detail-content">
                            <div class="detail-label">Verification Status</div>
                            <div class="detail-value">
                                <% if(studentProfile.Verification_Status==='Verified') { %>
                                    <span class="badge-status badge-verified">
                                        <i class="fas fa-check-circle mr-1"></i> Verified
                                    </span>
                                <% } else { %>
                                    <div class="action-buttons">
                                        <span class="badge-status badge-pending">
                                            <i class="fas fa-clock mr-1"></i> Pending
                                        </span>
                                        <a href="/admission-verification/<%=studentProfile._id%>" class="btn btn-success btn-sm">
                                            <i class="fas fa-check mr-1"></i> Verify
                                        </a>
                                    </div>
                                <% } %>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <div class="section text-center">
                <a href="/TBSA/admin-dashboard" class="btn btn-back">
                    <i class="fas fa-arrow-left mr-2"></i> Back to Dashboard
                </a>
            </div>
        </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        function editReceipt() {
            const currentNumber = "<%=studentProfile.Cash_Recipt_Number%>";
            const newNumber = prompt("Edit receipt number:", currentNumber);
            if (newNumber && newNumber !== currentNumber) {
                alert("Would submit: " + newNumber);
            }
        }
    </script>
</body>

</html>