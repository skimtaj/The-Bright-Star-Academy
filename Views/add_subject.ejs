<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add New Subject</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #E3F2FD;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }

        .subject-form {
            background: white;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.2);
            width: 100%;
            max-width: 500px;
        }

        .subject-logo {
            width: 90px;
            height: 90px;
            margin-bottom: 10px;
        }

        .btn-custom {
            background-color: #00A65A;
            border: none;
            color: white;
        }

        .btn-custom:hover {
            background-color: #008C4A;
        }

        .form-label {
            font-weight: bold;
        }

        .form-intro {
            text-align: center;
            margin-bottom: 15px;
            font-size: 16px;
            color: #555;
        }

        .underline {
            width: 100%;
            height: 2px;
            background-color: #00A65A;
            margin-bottom: 20px;
        }

        .back-btn {
            background-color: #6C757D;
            color: white;
            border: none;
        }

        .back-btn:hover {
            background-color: #5A6268;
        }
    </style>
</head>

<body>
    <div class="subject-form">
        <div class="text-center">
            <img src="https://cdn-icons-png.flaticon.com/512/2232/2232688.png" alt="Book Icon" class="subject-logo">
            <h3 class="mb-2">Add New Subject</h3>
            <p class="form-intro">Fill in the details below to add a new subject.</p>
            <div class="underline"></div>
        </div>
        <form method="post">
            <div class="mb-3">
                <label class="form-label">Subject Name</label>
                <input type="text" class="form-control" required placeholder="Enter subject name" name='subjectName'>
            </div>

            <div class="mb-3">
                <label class="form-label">Subject Code</label>
                <input type="text" class="form-control" required placeholder="Enter subject code" name='subjectCode'>
            </div>

            <div class="mb-3">
                <label class="form-label">Subject Type</label>
                <select class="form-control" required name='subjectType'>
                    <option value="">Select Type</option>
                    <option value="Theory">Theory</option>
                    <option value="Practical">Practical</option>
                </select>
            </div>

            <div class="mb-3">
                <label class="form-label">Subject Teacher</label>
                <select class="form-control" required name='subjectTeacher'>
                    <option value="">Select</option>

                    <% teacherSourse.forEach((tech)=>{ %>

                        <option value="<%=tech.Name%>">
                            <%=tech.Name%>
                        </option>

                        <%}) %>

                </select>
            </div>

            <button type="submit" class="btn btn-custom w-100">Add Subject</button>
            <a href="/TBSA/admin-dashboard" class="btn back-btn w-100 mt-2">← Back</a>
        </form>
    </div>
</body>

</html>